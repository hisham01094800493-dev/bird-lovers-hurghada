{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "express",
  "buildCommand": "pnpm build && pnpm exec esbuild api/index.ts --bundle --platform=node --packages=external --format=esm --outfile=api/index.js",
  "installCommand": "pnpm install --frozen-lockfile"
}
